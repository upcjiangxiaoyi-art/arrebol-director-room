# 更新日志 · Arrebol D 暗河红霞导演系统

> 这份日志此前挤在 `manifest.json` 的 `description` 字段里，
> 累积到 4321 字——酒馆扩展面板会把它整段渲染出来，成了一堵墙。
> v1.22.0 起搬到这里，原文一字未改，只做归位。

---

## v1.37.0

浮标动效开关（提议：江，转述用户「手机发烫怀疑是浮标动态引起的」；施工：波哥 Claude Fable 5.1）。浮标那两道水波和一段亮流是永不停的 CSS 动画，加上按钮上的 drop-shadow 滤镜，iOS Safari 的合成器确实会一直有活干；是不是发烫的元凶不好拍胸脯，给个开关让人自己对比最省事。共享设置里「显示小红霞浮窗」下面多一勾「浮标流动动效」，默认开，浮窗与内嵌抽屉两端镜像。关掉只做一件事：给浮标挂 data-arb-motion="off"，style.css 据此把水波、亮流、脉冲一律 animation: none，效果等同系统「减少动态效果」，四段渐变底和玻璃高光原样保留；按钮与拖动监听不重建，位置不丢，切换立即生效。设置存在 fabMotion。测试：test_ui.js 129 → 134 项（两端都有且默认开、关掉立即静止并保存、不重建浮标、再开恢复）。

## v1.36.3

晴空粉霞主按钮同调（提议：江「每个按钮的颜色也能一起压一下吗」；施工：波哥 Claude Fable 5.1）。浮标压成粉为主之后，「分析」「把这份稿挂上」「试抽一张」那几枚玫红→紫→宝蓝的实色按钮就显得太冲了。把 --arb-primary／--adrx-primary 改成霞粉 #F8C6D4 → 玫粉 #EE97B0 → 尾巴一点蓝 #9DABD8，hover 各亮一档；底色轻了，按钮字从纸白改落墨 #2B3352，与其余浅色套一致。胶囊开关、方形勾、计数条同步改成玫粉→尾巴一点蓝。头部三色霞线、结果卡竖条、滑杆玫红不动。真浏览器截图核对浮窗与内嵌抽屉。

## v1.36.2

晴空粉霞浮标重调（提议：江「浮标有点太自由平等博爱了」；施工：波哥 Claude Fable 5.1）。v1.36.0 的浮标四个停点是霞粉→玫红→紫→宝蓝，三色各占三分之一，挂在聊天上真像一面国旗。渐变停点的位置钉在 SVG 里（0／.38／.72／1），CSS 只能换颜色，于是把中段也换成粉：霞白 #FDEAEF → 烟粉 #F6B9CA → 玫粉 #EA8AA6 铺满前四分之三，宝蓝退成 #6F86C4 只在尾巴上收一下；底色轻了，ARB 从纸白改落墨 #2B3352，水波线换玫粉两端渐隐。面板不动。真浏览器截图核对。

## v1.36.1

test_stream.js 时序抖动修复（提议：江「这个比配色更致命」；施工：波哥 Claude Fable 5.1）。现象：SPEED=10 加速跑时「导演 · 流式响应」那段时灵时不灵，导演请求还没发出去就去读 calls[0]，在 main 上同样 5 跑 3 败，真实时间跑基本能过。根因不在插件，在测试桩子。抓现场看到：失败时聊天里已有 3 轮，导演账上的基准线也是 3，主档与镜像一致、没被谁冲坏——它只是记晚了。桩子在第 1 轮落地后 4.2 秒（加速后 420ms）安排一次检查来立基准线，之后才塞第 2、3 轮；可前四段「收集器」各起了一个 jsdom 窗口，用完既不 killPoll 也不关，四个还活着的窗口各自跑初始化、建抽屉、轮询，加速后全挤在同一个事件循环里，导演窗口那次检查被拖到 +1022ms 才跑，此时第 2、3 轮早在 +601ms 塞进去了，基准线记成 3 而不是 1，3−3=0 不到 N=2，请求自然不发。真实时间下这个余量是 1.8 秒，几乎撞不上，所以看着像随机抖。修法：桩子多一个 stop()（清轮询 + 关窗口），十段每段结束都调，原来的 killPoll() 一并换成 stop()；另照 test_steps.js 在 build() 里先 localStorage.clear()——jsdom 的 localStorage 按域名跨窗口共享，上一段的设置备份与聊天镜像会漏进下一段，这次没作祟，但清掉是卫生。断言与用例一字未动，插件一行未改。验证：SPEED=10 连跑 8 遍全过，三进程并行压测全过，真实时间一遍全过。备案：test_hotfix／test_expand／test_tone／test_v124 同样不关窗口、不清 localStorage，本会话里每次都稳过，暂不动。

## v1.36.0

第七套配色「晴空粉霞」（提议：江；施工：波哥 Claude Fable 5.1）。烟粉刚落地江就趁热又给了一张三色卡——玫红 #E73E70、纸白 #FBFBFB、宝蓝 #3759A7，要一套白天的桃粉渐变，晴空粉霞（江自己说像法国国旗）。浅色组第五员，沿用「浅色底 + 变量覆盖」：style.css 末位新增 [data-adr-palette="skyrose"] 令牌层（面板、放大编辑器、内嵌抽屉三处同源），面子玫红压左上、纸白留中、宝蓝收右下；主按钮改成玫红→紫→宝蓝的实色渐变、落白字（其余浅色套都是淡底深字，这套颜色饱和，白字才压得住）；头部霞线玫红→纸白→宝蓝三色横过去，标题珠也是三色；胶囊开关、方形勾、计数条走玫红→紫→宝蓝；幕布薄纱给一层淡藕紫。浮标四个渐变停点霞粉→玫红→紫→宝蓝，水波线近白，ARB 落纸白。主题循环七套：夜色 🌙 → 粉霞 🌸 → 雾珠 🫧 → 天青 🕊️ → 红霞 🌹 → 烟粉 🎀 → 晴空 🌤️ → 回到夜色。真浏览器截图核对浮窗、内嵌抽屉与浮标。测试：test_ui.js 123 → 129 项（七循环逐站、晴空属浅色组的浮标断言、图标与提示）；test_theme.js 改为七循环；其余 8 套按真实时间回归全过。另记一笔：test_stream.js 在 SPEED=10 下时灵时不灵（导演请求还没发出就断言 calls[0]），在 main 上同样 5 跑 3 败，与配色无关，真实时间跑是过的，留待单独修。

## v1.35.0

第六套配色「莫兰迪烟粉」（提议：江；施工：波哥 Claude Fable 5.1）。此前五套各有各的脾气，独独没有一套莫兰迪；江给了一张粉色雷鸟的四色卡——Birch Blossom #DEC4C9 铺面、Muted Hearth #977272 点睛、Woodsmoke Shadow #594E4C 压影、Midnight Forest #181717 落字——要一抹粉的霞光、阴天的暗夏。它是浅色组的第四员，实现沿用「浅色底 + 变量覆盖」：style.css 末位新增 [data-adr-palette="morandi"] 令牌层（面板、放大编辑器、内嵌抽屉三处同源），主按钮烟粉→炉灰的实色渐变、聚焦环与光晕取炉灰、结果卡竖条炉灰到烟粉；头部霞线改为烟粉→炉灰→木烟，标题珠、运行状态点、进度卡顶线一并换色；其余浅色套钉死的粉→紫→蓝胶囊开关、方形勾与计数条在烟粉里太跳，逐条改成烟粉→炉灰；幕布薄纱给一层烟灰粉。浮标四个渐变停点霞粉→烟粉→炉灰→木烟，水波线用木烟色、两端渐隐，字色落墨。主题循环六套：夜色 🌙 → 粉霞 🌸 → 雾珠 🫧 → 天青 🕊️ → 红霞 🌹 → 烟粉 🎀 → 回到夜色；选择照旧存在 themePalette。真浏览器截图核对浮窗、内嵌抽屉与浮标。测试：test_ui.js 117 → 123 项（六循环逐站、烟粉属浅色组的浮标断言、图标与提示）；顺带修了 test_theme.js 自 v1.31.0 起的自身失败（配色表抽成了函数外的变量，测试只截函数没截变量），改为六循环逐站；其余 8 套 415 项回归全过。

## v1.34.3

展开温度可调、出厂提示词定锚女性向言情（提议：江；施工：波哥 Claude Fable 5.1）。① 展开抽屉多一格「展开温度」（0–2，默认 0.9）：规矩钉死之后温度高一点是好事，同一张卡不同局展开得不一样，不千篇一律；越界封顶。② 用这个插件的大多是女孩子，大部分用户懒得改提示词，所以出厂版直接定锚女性向言情：一切外部事件都为关系服务（制造不得不靠近、不得不同处、不得不合作的处境，或一点距离，或让旧物旧账浮上来）；给角色表现的机会而不是给他台词；给她选择的余地，每一幕留一个她可以接、可以不接、可以换个接法的口子；心动要具体（体温、距离、视线、一件被记住的小事）；忌打打杀杀的大男主戏、权谋跑偏、无关支线、为虐而虐、把她写成没有主意的人。「只给事件不给反应」照旧。测试：test_steps.js 80 → 83 项。

## v1.34.2

展开提示词改言情责编取向（反馈：江「跑起来很顺，但剧情风马牛不相及、神神叨叨」；施工：波哥 Claude Fable 5.1）。原先那版是照「把一段戏演丰富」写的，分析段还让模型先想场地天气能长出什么事——模型拿着角色卡的大设定往外编，越编越玄。现在换成言情责编的思路，硬规矩六条：事件卡是脊柱，每一幕都在演这张卡本身，不另起新线、不借题发挥；合情理，事件必须是这个世界、这个身份、这个地点此刻可能发生的事，不搞超自然、不玄乎、不神神叨叨、不堆巧合、不天降大事，一幕只发生一件小而具体的事；不引入新的重要人物，要人就用已有的或只有功能的路人；紧扣当前剧情，第一幕从正文停下的地方长出来，不跳时间不换地点；紧扣角色卡与世界观，它们只用来判断什么合情理、不是拿来编新线的；只给事件不给反应照旧。分析改为四问：此刻、落法、可用的人和物、按言情节奏怎么分幕（起因、靠近的机会、小波折、推力、余味；波折要小而真，不要生死大事）。用户提示词里把「最近正文 · 此刻」挪到角色卡前面，先把模型钉在正文停下的地方再给设定；温度 0.7 → 0.5。测试：test_steps.js 77 → 80 项。

## v1.34.1

分幕只给事件、面板里能改（提议：江；施工：波哥 Claude Fable 5.1）。实测大成功之后的两处细节：① 展开提示词加一段硬性规矩「只给事件，不给反应」——分幕写的是这一幕世界抛给两个人的东西，不是两个人怎么接：不写「两人抱在一起」，写「雨突然下大了，山道上只有一个能站两个人的岩缝」；不写「他决定去找她」，写「她怀孕带球跑了，他此刻并不知道」。反应、情绪、动作、台词、决定一概不预设，角色按人设当场接，用户那一侧永远留给用户。原先「一句能说出口的话」那条删了；贴耳语同步改口。② 照小萤火，分幕在「耳边这张卡」面板里露出来：每幕一行，小标题和正文都能改，改了字自动存，正贴着的那一幕改了立刻重贴；▲▼ 调序（当前幕跟着它的正文走，耳边序号跟着变），✕ 删（至少留一幕；删了当前幕就换下一幕贴上），＋ 追加一幕（带占位正文），↻ 重新展开这张卡（再问一次展开器，从第一幕重贴）；导演的分析折叠在下面。正在改字时不重画，免得把手上的字冲掉。测试：test_steps.js 59 → 77 项，其余回归全过。

## v1.34.0

剧情展开推进（提议：江；用户反馈：床；设计参照：小萤火 v3.9.0 导演椅；施工与测试：波哥 Claude Fable 5.1）。用户反馈原话：「小风铃的剧情条要是展开写能写出很丰富的剧情，但子涵每次都是一层完结」「我想推一段男女主去登山的剧情，子涵一层就给我登完回家了」。不是模型坏，是它看见了整段，看见了就想演完。解法：**演员手里只有这一幕。** ① 抽卡页新增「🎬 剧情展开推进（可选）」抽屉，默认关。开了之后投卡时先让一个大模型（**独立 API 位**，地址／密钥／模型／加载模型／测试连接，不借小眼睛也不借统筹）按出厂提示词先写【分析】——紧扣角色卡人设、世界观、当前剧情，基调是第一要义，再按小说套路排节奏——然后严格切成 N 幕（2–12，默认 5）；分析强制不许省。提示词可改，留空用出厂，一键恢复。② 之后每一回合只把「这一幕」贴到耳边，固定 depth 0（生成前最后一句，不读注入深度设置）；正文只含第几幕、小标题、上一幕的名字、这一幕的正文，末尾一段贴耳语「本回合必须走到这一幕并演实；只演到这一幕为止，之后发生什么你不知道」。后面的幕不在上下文里，抢也抢不到。③ 推进不问模型，走回复位：贴上 → 回复落地记「用过」（重抽不多算）→ 用户再发言才结算翻页（连发不吞）；「每幕演几轮」可调，默认 1。耳边卡面板列出全部分幕并标出当前，三枚按钮：上一幕、下一幕、撤下这条线；「这张已兑现」也撤下分幕。④ 跑线期间常规 N 楼投卡与半衰期都让路；最后一幕演过自动结案、耳边清空、基准线移到当前楼，下一张按原节奏来。换聊天、暂停恢复都重贴当前幕。⑤ 统筹的投卡史里多一段【展开推进】：切成几幕、演到第几幕，只给当前幕正文，后面的幕只给名字，叮嘱别把后面的幕透给演员。⑥ 兜底：展开器失败或超时按整张卡投，状态行说明；NSFW 卡默认不展开（卡面要出门，走审核有被拒风险），单独勾选才展开；展开期间切了聊天本次作废。解析皮实：找 "steps" 键往前恢复 JSON，代码块、分析段含花括号都能解，多给截到 N，少给照收并警告。展开请求走流式接收（尊重流式开关），思考模型也认。测试：test_steps.js 新增 59 项（解析十种形状、投卡展开与 depth 0、回复位推进、每幕两轮、手动前后撤下、手动结案与暂停、换聊天重贴、失败回退、NSFW 不展开、统筹视野、面板字段），回归原有 474 项。写测试抓到两处桩子问题：jsdom 的 localStorage 按域名跨窗口共享（上一段的聊天镜像会漏进下一段）、盲抽三格混着 NSFW 库会让断言时灵时不灵。

## v1.33.0

浮标改版「流淌的暗河」（提议：江；施工：波哥 Claude Fable 5.1）。江的要求：浮标颜色跟主题走、只留流淌的暗河不要旁边的小圆圆、每套都更渐变更仙气。改法：① SVG 重画——珠心、光环、日月三样全部拆掉，ARB 居中，底下两道错位漂流的水波（一道向左 7 秒一循环，一道向右 11 秒一循环，路径按 40 单位周期铺到画框外，平移无缝）加一段沿波行走的亮流；底色从三段渐变改为四段，上面盖一层玻璃高光。② 每一处颜色都挂类名，style.css 末位按 data-adr-palette 五套各自上色，比面板再轻一档、更透：夜色靛蓝→紫→淡紫→月白，粉霞粉→杏→藕→天，雾珠珠粉→藕紫→雾蓝→青白，天青雾白→天青→灰蓝→陶土，红霞墨玫→酒红→玫瑰→粉玫；水波在浅色下用配色的深一档、深色下用近白，两端渐隐。③ prefers-reduced-motion 下三条动画全停。按钮本身与拖动监听未动，只换了内部 SVG；旧的 .arb-fab-halo／core／moon／sun 规则留着无害，另加一条 display:none 兜底。真浏览器五套并排截图核对。测试：test_ui.js 117 项照常通过（含「浮标沿波行走的高光仍在」与「换色不重建浮标」）。

## v1.32.0

第五套配色「暗河红霞」（提议：江；施工：波哥 Claude Fable 5.1）。插件叫了这么久暗河红霞，终于有一套真正的红霞：取江给的五色卡——Rose Ink #3A071F 打底、Blush Wine #6F1F3B 起伏、Petal Dust #B65A7C 点睛、Powder Rose #E2A6BA 描边、Ivory Bloom #FFF0F5 落字。它是深色组的第二员：此前 JS 里"深色"就等于 dusk 一套，现改为 ADR_DARK_PALETTES 深色组判定，浮标、编辑器、壳体内联样式都按组走；壳体背景与墨色走自己的令牌，夜色那套内联值原样保留。style.css 末位新增 [data-adr-palette="wine"] 令牌层，dusk 钉死的几处紫（头部霞线、滚动条、下拉项底色、聚焦环、分析／挂稿按钮、运行状态点、卡池芯片、标题珠）逐条改成酒红；内嵌抽屉给一版象牙粉的浅色（抽屉一向是浅色，与夜色同理）；浮标四个渐变停点、珠心、字色、水波线同步。主题循环五套：夜色 🌙 → 粉霞 🌸 → 雾珠 🫧 → 天青 🕊️ → 红霞 🌹 → 回到夜色。真浏览器截图核对红霞与夜色两套。测试：test_ui.js 112 → 117 项（五循环逐站、红霞属深色组的浮标断言），其余 8 套 409 项回归全过。

## v1.31.1

结果卡竖条跟配色走（提议：江；施工：波哥 Claude Fable 5.1）。「情感导演结果」「统筹结果」标题左侧那根 3px 竖条此前钉死粉→蓝紫，四套配色下都是紫的；现改为 --arb-rose 到 --arb-sky 的渐变、光晕取 --arb-glow：天青下是陶土到天青，粉霞、雾珠、夜色各随其令牌。真浏览器截图核对天青与夜色两套。

## v1.31.0

第四套配色「天青如梦」（提议：江；施工：波哥 Claude Fable 5.1）。取自江给的一张水彩：上半是雾灰天青 #8DAEC3，下半是如梦令暖陶土 #AD9484，中间一圈留白。主题按钮的循环从三套变四套：暗河夜色 🌙 → 粉霞水光 🌸 → 雾珠月汐 🫧 → 天青如梦 🕊️ → 回到夜色；选择照旧存在 themePalette，重新打开保留。实现沿用 v1.30.2 的"浅色底 + 变量覆盖"结构：style.css 末位新增一层 [data-adr-palette="celadon"] 令牌（面板、放大编辑器、内嵌抽屉三处同源），头部霞线改为天青→留白→陶土，标题珠、运行状态点、进度卡顶线一并换色；浮标 SVG 的四个渐变停点、字色、水波线同步（顺手给珠心渐变的最外一停加了类名，其余三套不受影响）。两处 JS：壳体墨色从钉死的 #404D62 改为跟 --arb-ink 走（粉霞与雾珠的令牌值与原来几乎一样，肉眼无差）；幕布薄纱按配色取色，天青用冷雾，其余浅色仍是粉霞那层。顺带修了 main 上 v1.30.2 遗留的 test_ui.js 自身失败：三循环后"点一下就回夜色"的旧断言已经不成立，改为沿四循环逐站断言面板、抽屉、浮标与设置四处同步。真浏览器截图核对四套配色各一张。测试：test_ui.js 从 94 项增至 112 项，其余 8 套 409 项回归全过。

## v1.30.2

三色主题切换（ripple & GPT）：保留暗河夜色、粉霞水光，并恢复旧浅色雾珠月汐。点击浮窗右上角主题按钮依次切换；选择沿用设置保存与本地备份，重新打开后保留。浮标同步主题，浅色设置抽屉与放大编辑器同步两套浅色配色。旧的开灯设置自动对应粉霞水光，关灯仍为暗河夜色。切换只监听原生 click，避免手机 touchend 与 click 连跳；按钮提供当前和下一套主题名称。新增无需依赖的 `node test_theme.js`，验证旧偏好兼容、循环切换、保存恢复与界面同步。导演、计数、API 和抽卡管线不变。

## v1.30.1

流式接收减轻内存占用（ripple & GPT）：SSE 数据解析后不再额外保存整份传输原文，只保留最多 512 字符的诊断片段、未收完整的事件、导演正文、思考字数和错误信息。纯前导空白不再累积；中转直接返回整份 JSON 时仍保留完整响应解析。没有增加回复长度或思考时长上限，没有截断导演正文；聊天缓存、计数、重载与 UI 不变。新增长思考、长正文、分块 JSON、诊断样本之后的错误和末尾事件回归测试。此改动减少已解析 SSE 的额外占用，不保证消除 Safari 白屏，异常未结束事件与整份 JSON 仍需完整缓冲。

## v1.30.0

基调仓（提议：江；施工与测试：波哥 Claude Fable 5.1）。两位导演此前只认角色卡：卡写将军，就往家国天下调；卡苦大仇深，就往虐里调。可同一张卡，有人想玩两小无猜、有人想搞笑、有人想国破山河在、有人纯搞色——卡不知道，用户知道。现在共享设置最上面多一块「这局的基调」：① 一个下拉从基调仓里选一条填入，一个文字框自己写或改，一个名字框加三枚按钮（存进基调仓／从仓里删掉／清空这局基调）。② 基调仓是账号级的，出厂八条（轻松日常、甜甜恋爱、两小无猜、搞笑沙雕、慢热细水、虐恋深情、家国大义、纯欲向），按名补齐一次、删过不复活（toneSeeded 记账，同信封仓的规矩）；自己存的条目随账号走。③ 这局用哪条、改成什么样，存在聊天文件（chat_metadata 的 arrebol_d.tone，同 v1.24 导演记忆），换聊天各记各的，换设备不失忆；换聊天时面板自动回填，正在打字的那个框不动。④ 喂给导演的方式：采买清单最前面一段【用户基调 · 第一要义】——放在一次性补充指令之前（它是长期的框，补充指令只是这一拍的临时插话）、角色卡之前；正文写明「压过角色卡、世界书、人设与此前剧情所暗示的一切调子：卡写得再苦大仇深，用户说想谈恋爱，指导就得帮着谈恋爱」「与基调相悖的方向哪怕更符合原卡也不要给」「正文偏了要拉回来」；结尾指令补一句「所有方向以【用户基调】为第一优先」；系统消息再钉一句【硬性】，用户预设改成什么样都压不掉。留空＝不设基调，提示词一个字不提，导演照卡走。⑤ 只喂两位导演；小眼睛（择池／择卡）不看基调，它的「整体调性」另有来源。测试：test_tone.js 新增 44 项（两位导演都吃到且在最前、没基调一字不提、基调在补充指令之前、出厂八条就位、选一条即填入并存进这局、打字即存、存仓、删仓不复活、清空、换聊天回填），回归原有 365 项。

## v1.29.4

NSFW 库投不出热修（报告：用户反馈经江转达；复现、施工与测试：波哥 Claude Fable 5.1）。反馈是「小风铃 NSFW 部分完全不触发，单独打开也不触发」。先用 jsdom 桩子复现：盲抽只开 NSFW 是正常的，问题全在择池／择卡——① 小眼睛一失败就降级盲抽，降级时 NSFW 一律不参与（v1.19.2 的闸），只开 NSFW 时等于「这一轮空过」，而失败是确定性的（DeepSeek 审核打回情欲正文、或答复无效），于是永远空过，日志里一行行「择池没成功，而此刻只有 NSFW 库可抽」。② 三格全开时更拧：双向硬门只在【刚刚这一楼】已是情欲场面时才让选 NSFW，可小眼睛看到情欲正文就被审核打回（DeepSeek「Content Exists Risk」），一打回就降级，一降级就没有 NSFW——「该来的时候」恰恰是「永远不来的时候」。③ 只开 NSFW 时提示词还在注入硬门：「不满足就当它不在名单上、宁可选次贴合的」＋名单里只有它＋「必须选一个」，三句自相矛盾，模型只能答废话，答废话又降级。四处修：(a) 名单只有一个池（择池）／候选只有一张（择卡）时不问小眼睛直接投——没什么可挑，省一次调用，也不给审核打回的机会；(b) 名单或候选全是 NSFW 时不注入双向硬门，「必须选一个」照旧；(c) 只开 NSFW 仓库时降级闸不再排除 NSFW——那是用户自己的仓库配置，插件不替用户做选择；三格全开时网络断、超时、答废话仍按老规矩排除 NSFW；(d) 小眼睛被内容审核打回（4xx 带审核字样：DeepSeek「Content Exists Risk」、content_filter、SAFETY 一类，或 200 空答复带 finish_reason=content_filter）按「这一楼本身就是情欲场面、门已开」处理，在 NSFW 库内盲抽，投卡史记成「审核判 NSFW」，状态行直说「小眼睛被审核打回，按情欲场面已开门在 NSFW 库内随机给的」。这一条是启发式：审核也可能因暴力等内容打回，那种情况下会给错一张 NSFW，与盲抽 1/N 硬掷同价，日志与状态行都会说明。测试：test_nsfw.js 新增 34 项（闸与审核识别的纯函数、只开 NSFW 三种模式、两个 NSFW 池的提示词与降级、三格全开审核打回、三格全开其他失败仍排除 NSFW、硬门照旧注入），回归原有 271 项，另跑通 main 上新增的 test_ui.js。
## v1.29.3

运行总开关移到内容最上方，接着是导演切换和进度；「自动触发设置」更名为「独立开关与层数」。浮窗和内嵌抽屉同步调整，沿用现有总开关、独立开关和计数逻辑。

## v1.29.2

轻收纳：两位导演的 API 配置、预设改为默认折叠卡片，保留结果区直接可见；重新对表从自动触发设置中移到进度卡下方，沿用原按钮绑定与二次点击确认。

抽卡页同步使用记忆开合的折叠卡片：常用操作与当前卡片默认展开，仓库、卡库编辑、API、信封与说明默认收起。保留控件 ID 和保存流程，延续秀气字号、日夜配色与浮标波浪。修正折叠区输入框横向边距与宽度叠加导致的溢出。

验证：`test_ui.js` 92 项通过，JS / CSS 语法检查通过。覆盖两端折叠默认状态、开合记忆、API 编辑保存、顶部对表二次确认与进度同步；无 API 请求。未完成浏览器实屏复验。

## v1.29.1

根据手机截图收细界面：输入区 13px、进度数字 24px，减轻标题与按钮字重，缩小卡片留白、页签与控件的视觉尺寸；保留雾珠月汐配色及顶部工作台布局。

恢复浮标小波浪的流动高光。修正日夜含义：浮标始终跟随面板太阳 / 月亮按钮，移除时钟模式与定时器，已有时钟偏好直接忽略；换色不重建浮标、不改位置，减少动态效果设置仍生效。

验证：`test_ui.js` 57 项、`test_expand.js` 22 项通过，JS / CSS 语法检查通过；覆盖旧设置兼容、面板与浮标同步、节点与位置保留、波浪节点、顶部布局和进度交互。未完成浏览器实屏复验。

## v1.29.0

雾珠月汐（需求与方向：ripple；施工：GPT）。朝霞使用粉白珍珠 / 晨雾淡紫，暗河使用蓝紫海面 / 月光；强化顶部计数卡的边缘柔光，减轻设置卡片与 API 预设套框，保留顶部切换与进度布局。

浮标改为日月珠光胶囊，默认按设备当地时间自动变色：7:00–19:00 为日间，其余为夜间。每分钟只检查配色，页面恢复可见或焦点时立即同步；只有颜色改变才更新主题属性，不重建 SVG、不修改浮标位置、不发送请求。共享设置可改成跟随面板明暗。自动时钟只安装一份；减少动态效果时停用浮标装饰动画。

验证：`test_ui.js` 68 项、`test_expand.js` 22 项通过，JS / CSS 语法检查通过。包含昼夜边界、前后台切换、模式保存与双端同步、重复初始化、换色后节点 / 位置保留。浏览器仍无法访问本地预览，未完成实屏视觉验收。按用户本轮明确要求，完成检查后直接合并主支。

## v1.28.0

顶部工作台与 UI 精修（需求与方向：ripple；施工：GPT）。

- 浮窗和内嵌抽屉都以「情感导演 / 统筹 / 抽卡」切换开场，紧跟当前页进度。浮窗导航吸顶，切换后返回顶部；选中状态与进度在两个入口同步。
- 情感与统筹原有的触发开关、间隔、重新对表移到进度下的「自动触发设置」；共享设置默认收起。沿用开合记忆与原有控件 ID、自动保存链路。
- 朝霞 / 暗河继续沿用，进度数字放大，卡片层次减轻，计数技术信息折叠。数字未变时不重复重建计数 DOM，数字变化时保留详情展开状态；增加进度条与导航的辅助阅读信息。
- 小风铃新增同样的进度展示，读取已有 `lastDrawAt`，关闭、暂停和等待聊天时显示对应状态，不创建新计数账本。
- 修复「暂停投卡」少了触摸短锁的问题：共享设置的刷新可能先于 change 事件把开关拨回；现在与其他开关一样在按下时保护。放开浮窗与滚动区的硬性最小高度，避免短屏 / 横屏撑出可用区域；手机文本框使用 16px，减少 iOS 聚焦放大。
- 同步修正内嵌抽屉标题里残留的旧版号。导演 API、预设、注入、自动触发基准线计算及抽卡规则不变。

验证：`test_ui.js` 48 项、`test_expand.js` 22 项、`test_v124.js` 32 项通过；JavaScript / CSS 语法检查通过。本轮浏览器的本地 URL 访问受限，未完成手机实屏视觉验收，发布前需确认朝霞 / 暗河、滚动吸顶和横屏布局。

## v1.27.1

流式接收加开关（提议：江；施工：波哥 Claude Fable 5.1）。共享设置里「导演上岗前先打个招呼」下面多一枚「流式接收导演稿」，浮窗与酒馆内嵌抽屉都有，默认开、老存档没这个字段也按开处理。关掉就回到 v1.26 的老路：请求体 `stream: false`、Accept 只要 JSON，给不支持流式的中转站留门；收集器本来就认整份 JSON，两条路共用同一段解析与空正文文案。其余一字未动。测试：test_stream.js 新增 7 项（关掉后 stream:false 与照常注入、Accept 头、勾选框就位、默认开、改动即存），共 32 项；回归原有 239 项。

## v1.27.0

导演请求改流式接收（提议：江；施工：波哥 Claude Fable 5.1）。会思考的模型（DeepSeek-R1、Gemini thinking、Claude 开 extended thinking 一类）接在导演位上经常报错：非流式请求要等模型把整段思考跑完才回第一个字节，中转站／网关的空闲超时（常见 60–100 秒）在这之前就把连接掐断，回来是 502／524 或半截 JSON；本地那道 120 秒硬闸也常在思考结束前落下。现两位导演的请求改为 `stream: true`：① 逐块读 SSE，只拼 `delta.content` 进稿子，思考（`reasoning_content` / `reasoning`）看见就跳过、只计字数；状态行实时写「模型思考中，N 字」／「已收到 N 字」。② 120 秒从「整趟」改为「两块之间」的空闲闸，每来一块重新上表——思考再长，只要连接上有字节走就不掐；真断流才判「流式接收中断」。③ 服务端不理 `stream:true` 而回整份 JSON 时（老中转、测试桩）自动按原路解析，一字不丢；流里夹带的 `error` 事件、CRLF、chunk 切在 JSON 或多字节汉字中间都处理了。④ 正文空着时不再只留「无法解析响应」，会说明「模型只回了 N 字思考，正文一个字没出」「finish_reason=length，输出上限先到了」。请求体其余参数（模型、两条消息、temperature 0.6）、注入、计数、重试、拍子、打断按钮全部未动；小眼睛（择池／择卡／兑现）那几路本来就是几个字的短答复、有自己的额度说明，这版不碰。测试：test_stream.js 新增 25 项（收集器纯函数：思考跳过、chunk 切边界、CRLF、UTF-8 切半、整份 JSON 识别、流中报错；端到端：带思考的流式注入、整份 JSON 兼容、只回思考的失败文案、半路断流的空闲超时），回归原有 239 项。

## v1.26.1

放大编辑热修（报告：江；施工：波哥 Claude Fable 5.1）。iOS 上点开编辑器会「飞到上面去」：编辑器一开就自动聚焦输入框，Safari 弹键盘时把布局视口整个往上顶，position:fixed 的整屏层跟着页面一起被顶走，收了键盘也不回来，屏幕下半截露出底下的面板。两处修：① 编辑器改钉在可视视口（visualViewport）上——打开时按可视视口定位，键盘弹出就缩到键盘上方、页面被顶就跟着复位，关闭时监听全部摘掉；② 手机（粗指针）上不再自动弹键盘，点进去再打字，桌面照旧落焦点到末尾。顺带：幕布吃掉触摸滚动，不再把底下的面板一起拖走。测试：test_expand.js 新增 4 项仿 iOS 视口（打开定位、键盘上顶后钉住、收键盘回整屏、关闭摘监听），共 22 项；回归原有 217 项。

## v1.26.0

放大编辑（提议：江；施工：波哥 Claude Fable 5.1）。文字框改起来要在小框里上下拖，跟酒馆世界书那枚 ⛶ 一比就累。现在浮窗与酒馆内嵌抽屉里每个文字框（共享记忆、两位导演的预设与结果、卡库编辑区、两段信封）右上角都有一枚 ⛶，两个单行的「想对导演说的话」也有；点开是整屏编辑器——手机全屏、电脑居中大卡，16px 字、1.75 行距、右上角实时字数，标题取紧邻的 label，没有 label 的取所在分区标题（「情感导演预设」「编辑卡库」）。确定回填原框并派发 input / change，原有的自动保存、卡库 600ms 防抖写库、抽卡忙保护一条不落；取消与 ESC 不回填；Ctrl/⌘+Enter 等于确定；单行框回填时换行换成空格。实现上只在文字框外包一层 .adrx-ta 容器，id 一个不改；按钮不带 adr044- 前缀，不会被全按钮兜底与触控标记拦下。编辑器与浮窗共用一套令牌：浮窗里打开跟随当前暗河／朝霞，抽屉里打开一律朝霞浅色。测试：新增 18 项（test_expand.js：按钮就位与幂等、回填触发保存、取消 / ESC 不动、单行去换行、卡库写档、重开浮窗不重复），回归原有 217 项。

## v1.25.0

「琉璃暗河」换皮（拍板：ripple；施工：波哥 Claude Fable 5.1）。功能测着已经稳了，这版只动脸不动骨：① 暗河（🌙）改墨靛玻璃面——顶部一抹霞光晕、每张分区卡带一线内高光、外投影压深；朝霞（☀️）改瓷白纸面——淡粉淡蓝晕、砚蓝墨字、白描边壳。② 按钮分两级：分析／挂稿／试抽这几处主动作改为实色霞光渐变（深字、柔光投影、悬停微抬），其余退为玻璃描边；删除与打断再退一级成幽灵按钮。③ 输入域从 44px 收到 40px（下拉 42px）、圆角 12、加内阴影，聚焦时霞粉描边 + 3px 柔环。④ 开关换 40×22 带投影旋钮的胶囊，选中即霞光渐变；进阶抽屉里仍是方形勾。⑤ 页签成分段控件（凹槽底 + 浮起的当前页）。⑥ 计数卡数字用香槟金、进度条带光晕；小风铃状态行、耳边卡、投卡史、三仓库槽与芯片统一到同族圆角与描边。⑦ 头部标题的 D 用渐变字，珠光点加环；关闭／换灯按钮改圆形；壳体圆角 14→22、投影加深（这两处是 open 流程里的内联 !important，只能在 JS 改值）；悬浮胶囊的三色由砚蓝→蓝灰→藕荷改为墨靛→霞蓝→霞粉，落影更轻，悬停微抬。⑧ 自动分析弹窗改为与暗河同源的玻璃卡。⑨ 酒馆内嵌抽屉同步到浅色瓷面版。⑩ 朝霞的霞光晕按 ripple 反馈再拉亮一档：顶部粉霞、右上霞蓝、左下藕紫、右下一点桃色，卡片与输入域透明度各降一档让底色透上来，头部霞线加浓；内嵌抽屉同步。实现方式：style.css 末位新增一层令牌驱动的规则（选择器统一 html 前缀压过历史锁），DOM、ID、事件、存储、计数、注入、API、抽卡管线与 v1.20.0 的触控命中区／右缘安全缝一字未动；历史 CSS 层原样保留，债留到下次清。验证：jsdom 桩子真跑 index.js 生成浮窗与抽屉 DOM，Playwright 在 390 与 1100 两档宽度、两套主题、三个页签下逐屏截图核对；回归原有 217 项测试。

## v1.24.0

导演的记忆搬进聊天文件（拍板：ripple；施工与测试：波哥 Claude Fable 5.1）。① 两位导演的基准线（还有几楼触发）、跟组记录（自己此前下过的 3 条指导）、常驻贴耳稿、放养标记，此前全在浏览器 localStorage 按 chatKey 存——换设备、换网址、清一次缓存，导演就失忆：进度从头数、贴耳稿消失、不记得自己说过什么。现与抽卡模块同构：chat_metadata 主档随聊天文件走，localStorage 只留一份按 chatKey 的镜像做灾备。老存档第一次打开时从旧的四把钥匙里搬家一次（只搬属于这把聊天的那份），旧键原样留着，回退到 1.23 仍可用。② 副产品：靠"猜这是不是同一把聊天"活着的那一套退役——同角色跨聊天继承基准线、last-key 迁移、broad key 全部删除。它假想的"注入后 reload 会换 chatKey"在现在的 chatKey 取法下不会发生，倒是会误伤：打开一把同角色、更长、本机没记录的旧聊天，只要旧 base ≤ 新楼数就把别人的进度继承过去。现在新聊天一律从自己的当前楼数起步。窗口口径→全量口径的一次性校准、base 高出 count 二十楼的贴齐自愈、删楼位移这三条保留，只是账本换了地方。③ 计数显示改为只读。「还差几楼触发」那行字此前读的时候会顺手建账、校准、落盘，1.19.3 那场微任务风暴就是"显示反向触发写入"闹的，当时只堵了计数那一半；现在建账只在自动触发检查这一条路上做，第一次检查之前计数条如实写"等对表"。④ 面板手感两处：每次 settings() 都 getItem + JSON.parse 一整份备份（含整套卡库）——现按原文缓存、且只在同一份设置对象上合并一次；悬浮球的 MutationObserver 从盯整棵 DOM 树改为只盯 body 直接子节点，流式生成期间不再每个字都进回调。所有 LS JSON 读取统一走原文缓存。未动：CSS 债（等下次换皮一起清）、API key 存放方式、导演采买清单不设预算（ripple 定案：导演用大模型、要全量信息）。测试：新增 32 项（test_v124.js：基准线入档、换设备恢复、老存档搬家、两把聊天各记各的、显示只读、删楼位移、备份读取次数、观察器范围），回归原有 185 项，共 217 项。

## v1.23.3

小眼睛的答复额度（施工：波哥 Claude Fable 5.1；拍板：ripple 授权）。点池 30、择卡与兑现判定 10、连通自检 10——会思考的模型把这点额度先花在思考上，回来是空的，于是"点池答复无效"、永远降级盲抽，连自检都过不了。四处统一提到 200：答复照旧逐字校验、多一个字作废，只是不再把思考也掐死；对不思考的模型没有任何区别，它本来就只回那几个字。空答复时日志与自检会说明"额度被思考花光了，换不思考的模型或在服务商侧关掉思考"，不再只留一句"答复无效"。同时 README 写明：只开 NSFW 仓库时择池／择卡的名单只有一格，小眼睛只能选它——这是用户自己的仓库配置，插件不替用户做选择。

## v1.23.2

全盘审读热修（审读、施工与测试：波哥 Claude Fable 5.1；终裁：ripple）。把 8400 行从头读了一遍，下面每一条都先用 jsdom 桩子复现出旧现象、再证明新版不犯。抽卡侧：① 冷却区填 0 反而变成"最近 32 张全不复用"——`slice(-0)` 等于 `slice(0)`，整个近期史都被当成了冷却区；现 M=0 就是没有冷却，面板上限也从 99 收到与缓冲一致的 32。② 兑现判定把 NSFW 卡面全文发给了小眼睛——择卡那一路早就写明"卡面不出门"，这条路漏了；现 NSFW 卡到半衰期直接降级为背景，不问、不花钱、不外泄。③ 择卡候选里的 NSFW 池按文件顺序只取前两个，池三池四永远进不了候选；现先打乱再取。④ 生命周期推进没有互斥：问询小眼睛期间（最长 8s）若新卡已投下，回来会把新卡当旧卡降级或结案；现问询前记下是哪张卡、哪楼投的，回来先对账，对不上整段作废，另加一面旗防两趟检查并肩问询重复扣费。⑤ 自动触发的旗子此前要到导演真开跑才升，前面读全量、抽卡检查、问小眼睛几段 await 全是敞开的；现进门就升，被挡掉的检查记下来、跑完补查一次，信号不丢。⑥ 答复里的间隔号「・」「•」「･」认成「·」。导演侧：⑦ 采买清单里【用户名称】填的是 `name2`——酒馆里那是角色，`name1` 才是用户；情感导演拿着"用户叫角色名"去判"是否过度代演用户"。⑧ 注入只写 `mes` 不写 `swipes[swipe_id]`，在注入那一楼左右翻一次页导演稿就没了；现同写，放养撕稿同理。⑨ 每拍注入后整聊天重载（`reloadCurrentChat`：清空、重读、重渲染全部楼层、广播 CHAT_CHANGED），长聊天在手机上每拍卡一下、滚动位置跳；现只用 `updateMessageBlock` 重画改过的那几楼，老酒馆没有这个接口时仍整体重载。⑩ 导演一趟最长 120 秒，回来直接往"最后一个有正文的助手楼"里写，那楼可能正在流式生成——稿被下一个 chunk 冲掉，保存和重绘还会撞上生成中；现生成中把稿留在预览，等 GENERATION_ENDED 再挂，最多等 3 分钟。⑪ 失败首报与退避的拍子按 (聊天, 导演, 楼数, N) 建键，楼数每来一楼就变，API 挂着时每一楼都是"新拍子"：弹窗每楼弹一次、退避每楼归零；现按基准线建键，同一条基准线到结算前只算一拍。通用：⑫ API 地址凡不以 /v1 结尾一律硬补 /v1，Gemini 官方兼容口 `/v1beta/openai`、智谱 `/api/paas/v4`、火山 `/api/v3` 全被改成 `…/v4/v1`；现只在路径里没有版本段时才补。⑬ `esc()` 不转义引号，库名进 `value="…"`/`data-adrcd-lib="…"` 时含引号会撕开属性；现一并转义。⑭ manifest 的 description 又长到 5371 字——1.22.0 专门为"扩展面板渲染成一堵墙"把它搬去 CHANGELOG，1.23 两版塞回来了；现只留一句。⑮ 清掉六个从未被调用的函数（escapeHtmlForDetails / saveChatSafe / refreshMessageDom / adrCdMarkTopHistory / adrCdFindMountedSlot / adr048RemoveOldFloatingBits）与两处遗物，文件头版本号对齐。未动、留给 v1.24 拍板的：导演基准线／跟组记录／浮动稿仍在 localStorage 而非 chat_metadata（换设备即失忆，也是那一摞基准线启发式的病根）；导演采买清单没有总预算（内嵌世界书全部启用条目无上限）；计数显示函数仍会写状态；小眼睛 `max_tokens: 10/30` 对开着 thinking 的模型大概率空答复→永远降级盲抽，需实机验证。测试：新增 48 项热修回归（test_hotfix.js），并回归原有 137 项，共 185 项。

## v1.23.1

取消弃权 · 改口不叫 DS（报告：ripple；施工：五哥 Claude Opus 5）。① 热修：v1.23.0 的弃权位导致一路空过、一张卡都不投。根因是提示词写坏了——给出的弃权理由是「一场戏正到紧要处、一段对峙刚起势、情绪正在收束」，角色扮演里几乎每一楼都符合其中一条；又补了一句「空一楼没有代价，打断有」，等于告诉模型走出口比判断更划算。判断题不能配一个比判断更省事的选项。本版把择池与择卡的弃权位整个拆除：名单里不再有「此刻不投卡」，择卡不再接受 0；提示词改为「必须选出一个，即使没有特别贴合的，也要挑最不打断当下的那一个」。旧答复「此刻不投卡」现在按无效答复处理，降级盲抽，不停摆。存档里遗留的 passUntil 一律归零，升级后立刻解除未走完的空过窗口。② 面板与日志里的「DS」改称「小眼睛」——这个位置接的可能是 DeepSeek、Gemini 或用户自选的任何模型，用厂商名叫它是不准确的；小眼睛指的是角色，不是厂商。测试：37 项择卡与信封 + 56 项择池／择卡 + 44 项生命周期与触控，共 137 项。

## v1.23.0

择卡与看时机（提议：ripple；施工：五哥 Claude Opus 5）。① 新增择卡档：按仓库均摊摸出若干候选，连卡面一起给 DS，让它挑最贴合此刻的一张，或回 0 弃权。治的是择池只解决前半段的问题——池选对了，池内那张仍是纯随机，229 张里随机一张跟当下情境不搭很正常。候选构成：带卡面的仓库各摊一半，NSFW 一池一行占位且卡面一个字都不外传（避开审核，也避开泄露），其准入仍由双向硬门把关；语境顺序与择池一致（此刻→前情→候选→调性）。答复只认数字，越界或夹带解释一律降级盲抽，降级时 NSFW 不参与。② 新增出厂信封「看时机」：标准信封说的是「以下事件已然发生，织入正文」，等于命令模型当楼兑现——一张温情卡撞上正在吵架的一幕，崩就崩在这一句。新信封保留「必须发生」的分量，只把时点的决定权还给写故事的模型：「若此刻场面不适合它进场，就让它先在背景里发酵，等一个合适的时机」。③ 修出厂信封补齐逻辑：旧写法只在信封仓库整个为空时才铺预设，老用户早已存着三套，此后新增的出厂信封永远到不了他们手上——「看时机」等于发不出去。改为按名补齐一次并用 cdEnvSeeded 记账，用户主动删掉的不会复活。测试：37 项择卡与信封测试 + 60 项择池测试（含择卡）+ 44 项生命周期与触控回归，共 141 项。

## v1.22.0

择池归位（报告：ripple 转述社群；施工：五哥 Claude Opus 5）。更正：v1.21 的三条 NSFW 闸里有两条打错了靶——反馈全部来自 DS 监督（择池）档，而连抽软闸与 NSFW 出场频率档只作用于盲抽路径，对择池用户完全无效，本版整块撤除；盲抽连抽社群本来就无异议，不该由插件替他们做主。唯一保留降级硬闸。本版三处真修：① 语境顺序倒置——旧版把角色卡＋世界书＋Persona（最多 8000 字）压在请求最前面，决定『此刻』的最近正文排在其后；提示词嘴上说以正文为准，结构上却把最重的位置给了静态设定，成人向角色卡里的情欲描写因此持续把 DS 推向 NSFW。现改为 此刻→前情→名单→近期已投→调性，并把最近正文拆成【刚刚这一楼】与【再往前几楼】两段（此前十几轮糊成一团，DS 分不清哪段是现在、哪段是已经收场的戏），静态调性由 8000 字压到 2000 字并明标『不是此刻的依据』；NSFW 双向硬门同步改为只看【刚刚这一楼】。② 新增弃权位「此刻不投卡」——此前名单里没有『什么都不投』，DS 被逼着每次必选一个池，哪怕正在进行的场面不该被打断；弃权后用 passUntil 节流（隔 min(3,N) 楼再问），不会每楼白烧一次调用。③ 降级出声——择池失败降级此前完全静默，状态行现在直说『上一张是 DS 没应答时随机给的』，自检同步报告。测试：33 项 jsdom 择池流程测试（假 DS 接口，逐段断言请求体顺序与配额、弃权、节流、降级、答复清洗），并回归 v1.20 全部 44 项。

## v1.21.0

NSFW 三道闸（报告：ripple 转述社群；施工：五哥 Claude Opus 5）。起因：用户反馈盲抽会莫名其妙出 NSFW 且连抽。查明是四条机制叠加，不是运气——① 掷仓库是在启用的仓库之间均等，跟卡数无关，NSFW 点亮就吃 1/N（三格全开 1/3，两格 1/2）；② 那套 NSFW 双向硬门只写在择池提示词里，盲抽模式一道门都没有；③ 择池失败降级盲抽时走全部启用仓库，NSFW 恢复满权重且绕过双向门（v1.19.2 自己标的『已知未堵』）；④ 防惯性 streak 只在择池分支更新，且只在同仓库内换池，冷却区只防同卡面不防同仓库。本版修：降级盲抽一律排除 NSFW（DS 挂了＝没人判准入条件，默认不准入；排空则整拍空过并说明原因）；新增连抽软闸（上一张来自 NSFW 则下一张换仓库，只剩这一格时照常出卡，不锁死）；新增 NSFW 出场频率档位（正常／减半 1-N→1-2N／仅择池参与），只动这一格，专属与通用的均等承诺不变；投卡史新增 slot 字段，老存档按菜单名前缀还原；自检行报出当前档位与连抽闸状态。测试：25 项 jsdom 统计+流程测试（每档真掷 1800 次验频率，降级路径 20 拍真投卡验证），并回归 v1.20.0 全部 44 项。

## v1.20.0

两件事（报告：ripple；施工：五哥 Claude Opus 5）：① 剧情小风铃点得着——三仓库开关与芯片"要点两三次才挂上"共四处成因，一次修齐：芯片行此前是本页唯一没有忙保护的可点区（手指按下到 click 之间整行被 innerHTML 重画，click 的 target 会退回容器、名字取不到，整下点击白丢），现补忙保护并在按下那一刻先记住手指落在哪张卡上作兜底；三个仓库开关补"按下即上锁"（此前等到 change 才上锁，晚半拍）；芯片与开关的写入改成写完回读、不对就重试一次，三仓库下方新增回执行明说存没存进这个聊天；原生下拉按下即长免疫（到 change/blur 或 25 秒保险丝），此前 1.5 秒的窗口盖不住 iOS picker 停留十几秒，窗口一过任何刷新都会把 picker 当场掐掉；命中区 28px→44px，芯片 36px，并关掉双击缩放延迟。② 生命节奏三档——原"半衰期"勾选框升为下拉：半衰期（默认，行为不变）／只说一次／常驻。新增的"只说一次"针对 Gemini 反复复述同一段剧情：贴耳注入每次生成都会把同一张卡再念一遍，半衰期只放软后半程语气、念的次数一次没少；这一档让卡只在下一层露一次面就自动撤下，耳边留白 N-1 楼，第 N 楼再给新卡新剧情，那一层没接住就算了。DS 兑现判定在这档跳过不花钱，投卡史记为"只说了一次"而非冒充已兑现。老存档按 cdHalfLife 自动迁移（true→半衰期／false→常驻），cdHalfLife 继续镜像写入，旧读法不会读反。全套 44 项 jsdom 端到端流程测试通过。

## v1.19.4

双导演分账热修：情感／统筹的开关与间隔变更只初始化本类型 baseline；修复全新状态下先操作一侧会顺手给另一侧落起点、导致另一侧晚开启却偷跑进度的问题。全量楼数、触发阈值、成功／失败结算、启动热修与剧情小风铃均不改。（排查与施工：ripple & GPT）

## v1.19.3

启动热修：修复全历史计数刷新与计数 UI 互相回调形成的无限微任务闭环；同步计数入口改为纯读取，并发全历史读取改为 single-flight，事件密集时只补最后一次刷新。无 TavernHelper 时继续使用当前窗口计数；不改计数口径、baseline、导演、剧情小风铃、注入或自动触发规则。（排查与施工：ripple & GPT）

## v1.19.2

小风铃不打断：修日常剧情莫名冒 NSFW 卡——根因是防惯性机制，DS 连点同一卡池第 3 次即被强制踢去全仓库盲抽，三仓库均等下 1/3 落进 NSFW（实测复现 33.1%），与 DS 判断无关。三处修：① 择池提示词加 NSFW 双向硬门：不满足条件就当它不在名单上，满足则必须选它、其余池一律不选（打断正在进行的场面比选错池更严重），明确转场后自动退出；菜单里没有 NSFW 池时整段不注入；② 防惯性由「全仓库盲抽」收为「同仓库内换池」，不再推翻 DS 的仓库判断，单池仓库自动空转；③ 择池静态语境截断 4000→8000，让 DS 读得到角色卡/世界书里的准入调性（仅抽卡调用点，导演侧零改动）。自检「最近一张」的模式值新增「择池·防惯性换池」。（报告：ripple；施工：五哥 Claude Opus 5）

## v1.19.0

换季「砚蓝胭雾」：深紫家族整体平移为砚蓝 #404D62——暗皮壳底从可可紫夜换成砚蓝夜（#404D62→#333F51→#2A3443），朝霞皮墨紫字与全部透明度变体、淡紫描边、双皮阴影一并转轨砚蓝系；粉族（晨霞粉/纸粉/胭脂）、晨金、奶油字一处未动，与顶栏泊位胶囊同源成套。CSS+JS内联刷色双侧同步，布局与逻辑零改动。（报告：ripple；施工：波哥 Claude Fable 5）

## v1.18.0

归属制（概念设计：ripple）："挂入"回归人类直觉——放进箱子＝永久归属，只住自家仓库格，点亮/熄灭只管这局用不用，熄灭不流放；未分箱库虚线显示点亮即认箱，落点保存/导入即入住，老存档自动迁移

## v1.17.2

落点管保存：保存未挂载的卡库时按落点自动挂进仓库并启用，已挂载的原位即改即生效，回执明说去向——"选好落点→保存"从此就是完整的挂载动作

## v1.17.1

导入落点补漏：落点下拉此前无持久化，iOS 跳文件 App 往返触发面板重绘即被复位，导致"导入并挂进某仓库"只存库不挂仓；现落点账号级落盘、导入读存档不读 DOM、双面板镜像同步

## v1.17.0

三仓库多选+一库一槽：每个仓库槽从单选下拉改为点选芯片，可同时挂多副卡库（张数·副数实时显示，三段抽自动吃到全部池子）；挂进某仓库的库从其余仓库候选里消失，根治"专属库还在通用格子里当选项"；旧单库存档无痛迁移，改名/删除/导入全链路同步

## v1.16.3

删楼不罚楼：删楼层导致计数进度清零的老伤修复——真删除（MESSAGE_DELETED 事件/轮询缩水兜底）时基准线随差值平移，已过进度原地保住；partial 小读数的 no-shrink 保护照旧，抽卡小能手基准线同步位移

## v1.16.2

紧急修复：酒馆 dryRun 试跑也发生成开始事件且永无结束，导致 1.16.0 的等待旗卡死、到点不触发；现无视试跑，并增加收到完整楼层即降旗的第二重保险

## v1.16.1

无标签兔底：标签名填 * 即整层楼当正文读（可与具体标签混填做兜底，导演旧稿自动洗掉不回声，每楼截 2500 字）

## v1.16.0

两件事：① 正文标签开口子——进阶开关里可自定义正文标签名（默认 content，逗号分隔多个），适配各家预设；② 节奏对账——正在流式生成的楼数得着却读不着（标签未闭合提取为空，还挤占阅读窗口），现在自动分析等生成结束再开工，带 3 分钟防死锁保险丝

## v1.15.3

自由浮标缩小一号（32→28px，应社区反馈）

## v1.15.2

泊内小灯：泊位 overflow 裁外扩光圈，分析改由胶囊内侧胭雾呼吸灯提示，自由漂浮时光圈依旧

## v1.15.1

换装与点灯：浮标换上「砚蓝入胭雾」SVG 胶囊皮肤（流光过河动效，外壳透明化）；新增分析脉冲——导演分析进行中，胶囊亮胭雾光圈呼吸，成功/失败/超时/手动打断均正确熄灯；自由漂浮与泊入避风塘顶栏两种形态同样生效

## v1.14.5

两处修复：① 补拍静音的判定从「是谁叫醒的」改为「这一拍真的失败过没有」，修好轮询捡回的首次触发导演正常跑却不打招呼；② 抽屉与浮窗共用同一套 id，新增实时镜像，任一侧改动立刻同步到另一侧，修好在浮窗改设置后被抽屉旧值覆盖回滚（含 API Key）

## v1.14.4

朝霞初晓·开灯皮：浮窗头部新增 🌙/☀️ 小灯，一键在「暗河红霞」深色与「朝霞初晓」淡粉紫浅色间切换，选择账号级记忆；仅换皮，布局与全部功能一字未动

## v1.12.0

卡的生命周期：一张卡不再只有"投放"——挂过半衰期自动从「必须发生」退为「已发生的背景」；读到落地了可一键结案撤下（下一张仍按原节奏，中间留白让剧情喘口气）；可选让 DS 在半衰期问一句「兑现没」自动结案，判不了则安全降级绝不误撤；投卡史带兑现状态。信封升级为两段式＋按名预设（出厂标准/强硬/轻柔三套，对付不同模型的话术口味）。抽卡三仓库（专属／通用／NSFW 各自勾选、三段抽）与统筹、情感导演一字未动

---

## 署名

- Arrebol D 小红霞导演插件
- ripple & GPT & Claude
- v1.10-1.12 施工：波哥（Claude Fable 5）

---

## 测试

仓库里的 `test_flow2.js` 与 `test_pick.js` 是端到端流程测试，
不会被酒馆加载（`manifest.json` 只声明了 `index.js` 与 `style.css`）。
改动抽卡或择池链路后，本地跑一遍再发版：

```bash
npm install jsdom
node test_flow2.js   # 卡的生命周期 · 三仓库触控 · 旧存档迁移（44 项）
node test_pick.js    # 择池语境顺序 · 弃权 · 节流 · 降级（33 项）
```

## 已知但暂不处理

- `#adr048-fab.adr048-analyzing` 的脉冲动画动的是 `box-shadow`，
  与本项目 CSS 注释里「动态效果仅保留 opacity / transform」的自订规矩相悖。
  改用伪元素 `transform: scale` 会被 `#adr048-fab` 自身的 `overflow: hidden` 裁掉，
  且按比例缩放会让光圈在宽药丸上左右扩得比上下多，形状走样。
  当前无实测卡顿，**不动**；若将来在低端机上看到分析时掉帧，从这里查起。
- 择池选中卡池后，**池内仍是纯随机**。卡池选对但那一张不贴合当下情境的情况仍会发生。
  彻底解法是「择卡」（让 DS 看若干候选卡面挑一张或弃权），改动较大，待定。
